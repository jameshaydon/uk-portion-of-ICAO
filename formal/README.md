# Lean translation

This directory contains a Lean 4 translation of the flight-plan extraction code
from the blog-post example.

Build it with:

```sh
lake build
```

Run the worked example with:

```sh
lake exe formal
```

The project is pinned to the installed Lean toolchain in `lean-toolchain`.
